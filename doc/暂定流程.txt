程序分前后台。

前台用QT，消息机制。

后台先加载。后台用数组，因为不必随时改动。

c++11说它的数据结构都很快，那貌似可以用vector。

以学生为单元。
学生类包括：名字，学号，每次是否到的一个一位数组。

我想可以导入导出excel。
导出这里应该有导出每次是否到的那些数据。

要两个线程。界面和声音。创线程我希望像JAVA那种的。
声音就要研究音色库，播放器。播放器应该有自带的。
